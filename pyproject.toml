[project]
name = "raw-packet-parser"
version = "0.1.0"
description = "A raw network packet analyzer written in Python."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "scapy>=2.7.0",
]
